import { ChangeDateTimeFromTextDirective } from './change-date-time-from-text.directive';

describe('ChangeDateTimeFromTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeDateTimeFromTextDirective();
    expect(directive).toBeTruthy();
  });
});
